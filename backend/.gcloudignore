# No subir al build de Cloud Run (equivalente a .gitignore para gcloud)
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Secretos y locales — NUNCA al contenedor
.env
.env.*
!.env.example

# Notas/artefactos locales
*.log
